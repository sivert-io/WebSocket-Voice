# Example client environment variables
# The client does not require environment variables for basic operation.
# Users add servers manually through the application interface.
# Authentication is automatically configured to use the official Gryt Auth API.
